class_name asdfasdf
extends Node

# Called when the node enters the scene tree for the first time.
func _ready():
	print('I am a test script')
	#var script_editor = EditorInterface
	#print(script_editor)


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
